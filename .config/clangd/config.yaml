# Project compile flags come from compile_commands.json. Keep the global
# configuration neutral so Apple clangd uses the compiler recorded by the
# project's native macOS build instead of mixing Apple Clang with GCC headers.
{}
